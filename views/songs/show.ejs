<!--
  Step 1: This is your descretion how you would like to display data,
  but you must list display only one document based on the passed ID.
-->
<%- include ('../partials/header') %>
<div class="container my-5">
    <header>
        <h1>
            <%= song.name %>
            <a href="/songs/<%= song.id %>/edit">
                <i class="fa fa-edit"></i>
            </a>
        </h1>
        <br>
        <small>
            <%= (song.updatedAt.toLocaleString("en-CA")) %>
        </small>
        <p><strong>By: <%= song.user.fullname %></strong></p>
    </header>
    <hr>
    <div class="content">
        <strong><%= song.name %></strong> was written in <%= (song.dateOfPublished.toLocaleString("en-CA")) %> by <%= song.artist %>.  
    </div>
</div> 


<%- include ('../partials/footer') %>